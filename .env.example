# ═══════════════════════════════════════════════════════════════════
# MobiTickets Backend - Environment Configuration
# ═══════════════════════════════════════════════════════════════════

# ───────────────────────────────────────────────────────────────────
# 🌍 Environment
# ───────────────────────────────────────────────────────────────────
NODE_ENV=development  # development | production | test

# ───────────────────────────────────────────────────────────────────
# 🗄️ Database Configuration
# ───────────────────────────────────────────────────────────────────
# PostgreSQL Connection String
DATABASE_URL="postgresql://postgres.uxxdeiviwlrjwqmalxxh:[57ws4l@xTL@$0nqv]@aws-1-eu-west-1.pooler.supabase.com:5432/postgres"

# For Prisma Postgres (managed service):
# DATABASE_URL="prisma://accelerate.prisma-data.net/?api_key=YOUR_API_KEY"

# ───────────────────────────────────────────────────────────────────
# 📦 Upstash Redis Configuration (Serverless)
# ───────────────────────────────────────────────────────────────────
# Get these from: https://console.upstash.com
# Create a new Redis database and copy the REST URL and Token
UPSTASH_REDIS_REST_URL="https://your-redis-name.upstash.io"
UPSTASH_REDIS_REST_TOKEN="your-upstash-redis-token"

# ───────────────────────────────────────────────────────────────────
# 📬 Upstash QStash Configuration (Serverless Message Queue)
# ───────────────────────────────────────────────────────────────────
# Get these from: https://console.upstash.com/qstash
# QStash is used for background job processing (emails, NFT minting, etc.)
QSTASH_TOKEN="your-qstash-token"
QSTASH_CURRENT_SIGNING_KEY="your-current-signing-key"
QSTASH_NEXT_SIGNING_KEY="your-next-signing-key"

# Webhook Base URL (your public URL that QStash will call)
# In development: Use ngrok or similar tunneling service
#   ngrok http 3001 → https://abc123.ngrok.io
# In production: Use your actual domain
#   https://api.mobitickets.com
# WEBHOOK_BASE_URL="https://your-ngrok-url.ngrok.io"

# ───────────────────────────────────────────────────────────────────
# 🔐 JWT Authentication
# ───────────────────────────────────────────────────────────────────
# Generate secrets with: openssl rand -base64 32
# NEVER use these examples in production!
JWT_SECRET="REPLACE-WITH-STRONG-SECRET-MIN-32-CHARS"
JWT_REFRESH_SECRET="REPLACE-WITH-DIFFERENT-SECRET-MIN-32-CHARS"

# Token Expiration Times
JWT_ACCESS_EXPIRATION="15m"   # 15 minutes
JWT_REFRESH_EXPIRATION="7d"   # 7 days

# ───────────────────────────────────────────────────────────────────
# ☁️ Cloudinary Configuration
# ───────────────────────────────────────────────────────────────────
# Get these from: https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME="your-cloud-name"
CLOUDINARY_API_KEY="123456789012345"
CLOUDINARY_API_SECRET="your-api-secret"
CLOUDINARY_UPLOAD_PRESET="mobitickets"

# ───────────────────────────────────────────────────────────────────
# 🌐 Server Configuration
# ───────────────────────────────────────────────────────────────────
PORT=3001

# ───────────────────────────────────────────────────────────────────
# 🔒 CORS Configuration
# ───────────────────────────────────────────────────────────────────
# Frontend URL(s) - comma-separated for multiple origins
CORS_ORIGIN="http://localhost:3000,http://localhost:3001"
CORS_CREDENTIALS=true

# Production example:
# CORS_ORIGIN="https://mobitickets.com,https://www.mobitickets.com"

# ───────────────────────────────────────────────────────────────────
# 🚦 Rate Limiting
# ───────────────────────────────────────────────────────────────────
RATE_LIMIT_MAX=100            # Max requests per window
RATE_LIMIT_WINDOW_MS=60000    # Window size in milliseconds (60s)

# ───────────────────────────────────────────────────────────────────
# 📧 SMTP Configuration (Optional - for email notifications)
# ───────────────────────────────────────────────────────────────────
# Leave empty for development (emails won't be sent)

# Gmail Example:
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER="your-email@gmail.com"
# SMTP_PASS="your-app-password"  # Generate at: https://myaccount.google.com/apppasswords

# SendGrid Example:
# SMTP_HOST="smtp.sendgrid.net"
# SMTP_PORT=587
# SMTP_USER="apikey"
# SMTP_PASS="YOUR_SENDGRID_API_KEY"

# AWS SES Example:
# SMTP_HOST="email-smtp.us-east-1.amazonaws.com"
# SMTP_PORT=587
# SMTP_USER="YOUR_SMTP_USERNAME"
# SMTP_PASS="YOUR_SMTP_PASSWORD"

SMTP_FROM="noreply@mobitickets.com"

# ═══════════════════════════════════════════════════════════════════
# 📝 SETUP INSTRUCTIONS
# ═══════════════════════════════════════════════════════════════════
#
# 1. Copy this file to `.env`: cp .env.example .env
# 2. Replace ALL placeholder values with your actual credentials
# 3. NEVER commit .env to git - it contains secrets!
# 4. Generate strong JWT secrets with: openssl rand -base64 32
# 5. Set up database: npx prisma migrate dev
# 6. Generate Prisma client: npx prisma generate
# 7. Start the server: npm run dev
#
# ═══════════════════════════════════════════════════════════════════
